#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DOTFILES_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

tic -x "$DOTFILES_DIR/terminfo/tmux-256color.terminfo"

echo "Installed terminfo: tmux-256color"
echo "If tmux is running, restart it: tmux kill-server"
